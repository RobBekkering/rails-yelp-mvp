<%= simple_form_for [@restaurant, @review] do |r| %>
  <%= r.input :content, placeholder: 'Review' %>
  <%= r.input :rating, placeholder: 'Rating', collection: (0..5) %>
  <%= r.button :submit %>
<% end %>
